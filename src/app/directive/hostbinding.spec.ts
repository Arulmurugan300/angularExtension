import { Hostbinding } from './hostbinding';

describe('Hostbinding', () => {
  it('should create an instance', () => {
    const directive = new Hostbinding();
    expect(directive).toBeTruthy();
  });
});
